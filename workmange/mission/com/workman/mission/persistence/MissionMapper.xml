<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.workman.mission.persistence.MissionMapper">

  <cache />
  
  <select id="getMission" parameterType="int" resultType="com.workman.mission.model.MissionModel">
  	SELECT M.ID ,
  	M.TITLE,
  	M.CONTENT,
  	M.CREATETIME,
  	M.HANDLETIME,
  	M.COMMITTIME,
  	M.TYPE,
  	M.STATUS,
  	M.NOTES,
  	SPONSOR_ID AS sponsorId,
  	SPONSOR_NAME AS sponsorName,
  	SPONSOR_DEP AS sponsorDep,
  	HANDLER_ID AS handlerId,
  	HANDLER_NAME AS handlerName,
  	HANDLER_DEP AS handlerDep
  	FROM MISSION M
  	WHERE M.ID=#{missionId}
  </select>
  <select id="getCount" resultType="int">
  	SELECT COUNT(ID) FROM MISSION M
	  	WHERE 1=1 
	  	<choose>
	  		<when test="missionCode != null">
	  			ID = #{missionCode}
	  		</when>
	  		<otherwise>
	  			<if test="type != null">
			  		AND M.TYPE = #{type}
			  	</if>
			  	<if test="startTime != null">
			  		AND M.CREATETIME &gt;= #{startTime}
			  	</if>
			  	<if test="endTime != null">
			  		AND M.CREATETIME &lt;=  #{endTime}
			  	</if>
			  	<if test="type != null">
			  		AND M.TYPE = #{type}
			  	</if>
			  	<if test="status != null">
			  		AND M.STATUS = #{status}
			  	</if>
			  	<if test="sponsorId != null">
			  		AND M.SPONSOR_ID = #{sponsorId}
			  	</if>
			  	<if test="handlerId != null">
			  		AND M.HANDLER_ID = #{handlerId} OR
			  		 EXISTS (SELECT 1 FROM MISSION_HANDLE MH WHERE M.ID = MH.MISSIONID AND MH.HANDLER_ID = #{handlerId})
			  	</if>
	  		</otherwise>
	  	</choose>
  </select>
  <select id="getMissions" resultType="com.workman.mission.model.MissionModel" 
  		parameterType="com.workman.mission.persistence.wrapper.MissionWrapper">
	  	SELECT M.ID ,
	  	M.TITLE,
	  	M.CONTENT,
	  	M.CREATETIME,
	  	M.HANDLETIME,
	  	M.COMMITTIME,
	  	M.TYPE,
	  	M.STATUS,
	  	M.NOTES,
	  	SPONSOR_ID AS sponsorId,
	  	SPONSOR_NAME AS sponsorName,
	  	SPONSOR_DEP AS sponsorDep,
	  	HANDLER_ID AS handlerId,
	  	HANDLER_NAME AS handlerName,
	  	HANDLER_DEP AS handlerDep
	  	FROM MISSION M
	  	WHERE 1=1 
	  	<choose>
	  		<when test="missionCode != null">
	  			ID = #{missionCode}
	  		</when>
	  		<otherwise>
	  			<if test="type != null">
			  		AND M.TYPE = #{type}
			  	</if>
			  	<if test="startTime != null">
			  		AND M.CREATETIME &gt;= #{startTime}
			  	</if>
			  	<if test="endTime != null">
			  		AND M.CREATETIME &lt;=  #{endTime}
			  	</if>
			  	<if test="type != null">
			  		AND M.TYPE = #{type}
			  	</if>
			  	<if test="status != null">
			  		AND M.STATUS = #{status}
			  	</if>
			  	<if test="sponsorId != null">
			  		AND M.SPONSOR_ID = #{sponsorId}
			  	</if>
			  	<if test="handlerId != null">
			  		AND M.HANDLER_ID = #{handlerId} OR
			  		 EXISTS (SELECT 1 FROM MISSION_HANDLE MH WHERE M.ID = MH.MISSIONID AND MH.HANDLER_ID = #{handlerId})
			  	</if>
	  		</otherwise>
	  	</choose>
	  	
  </select>
   <insert id="create" parameterType="com.workman.mission.model.MissionModel">
    INSERT INTO MISSION
      (TITLE,CONTENT,CREATETIME,TYPE,STATUS,SPONSOR_ID,SPONSOR_NAME,
      SPONSOR_DEP,HANDLER_ID,HANDLER_NAME,HANDLER_DEP,NOTES)
    VALUES
      (#{title},#{content},#{createTime},#{type},#{status},#{sponsorId},#{sponsorName},
      #{sponsorDep},#{handlerId},#{handlerName},#{handlerDep},#{notes})
      
      <selectKey resultType="int" keyProperty="id" order="AFTER">
   		SELECT LAST_INSERT_ID()
   	</selectKey>
  </insert>
  <update id="update" parameterType="com.workman.mission.model.MissionModel">
    UPDATE MISSION SET
    	<if test="commitTime != null">
    		COMMITTIME = #{commitTime},
    	</if>
    	<if test="handleTime != null">
    		HANDLETIME = #{handleTime},
    	</if>
      STATUS = #{status}
    WHERE CODE = #{code}
  </update>
  
</mapper>